A collection of command-line based Python projects built using core Python only.
These projects focus on logic building, file handling, and clean program structure, without using external libraries or OOP.
Each project is independent, CLI-based, and stored using text files.
